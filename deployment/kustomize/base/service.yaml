apiVersion: v1
kind: Service
metadata:
  name: alin-final-task-service
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: alin-final-task
  type: NodePort
